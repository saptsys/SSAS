import React from 'react';

const SoftwareValidationsDialog = ({ params }) => {
  return (
    <div>
      Invalid Software {JSON.stringify(params)}
    </div>
  );
};

export default SoftwareValidationsDialog;
